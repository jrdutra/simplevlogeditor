/* Compiler options used for the Web Workers of the app. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/worker",
    "lib": ["ES2022", "webworker"],
    "types": []
  },
  "include": ["src/**/*.worker.ts", "src/**/*.d.ts"]
}
